export interface User {
    email: string
    username: string
    password: string
}

export interface Product {
    id: string
    title: string
    description: string
    price: number
    brand: string
    category: string
}